#include<stdio.h>
int main()
{
    system("cls");
    int a;
    printf("enter the number:-");
    scanf("%d",&a);
    (a<5)? printf("a is less than 5") : printf("a is not less than 5");// short hand if and else.
    return 0;
}

// same thing is explained in file learn5.c