#include<stdio.h>
int main()
{
    int principal, interest, rate;
    system("cls");
    printf("hello \bworld");
    return 0;
}

// return 0 means program is successfully return
// ampersand(&) means 'address of' for compiler
// \b is an escape sequence which is for backspace.